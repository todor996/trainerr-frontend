import { TrainingTrainerLocal } from '@modules/trainer-pov/training/types/TrainingLocal.type.tpov';

const enResource: TrainingTrainerLocal = {
  rnd: 'Random Text in Eng',
};

export default enResource;
